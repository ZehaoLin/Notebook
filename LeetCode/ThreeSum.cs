//求nums中三数之和为0的集合，集合元素不重复
public class Solution {
    public IList<IList<int>> ThreeSum(int[] nums) {
        Array.Sort(nums);
        IList<IList<int>> list = new List<IList<int>>();
        if (nums == null || nums.Length == 0) return list;

        

        return list;
    }
}